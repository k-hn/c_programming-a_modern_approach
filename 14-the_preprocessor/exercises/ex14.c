#include <stdio.h>

//#define N = 10
//#define INC(x) x+1
//#define SUB (x,y) x-y
//#define SQR(x) ((x)*(x))
//#define CUBE(x) (SQR(x)*(x))
//#define M1(x,y) x##y
//#define M2(x,y) #x #y

int main(void)
{
    int a[= 10], i, j, k, m;	/* syntax error: a[= 10] */

    i = j;

    
    i = 10 * j+1;
    i = (x,y) x-y(j, k);	/* syntax error */
    i = ((((j)*(j)))*(((j)*(j))));
    i = (((j)*(j))*(j));
    i = jk;			/* syntax error */
    puts("i" "j");
    
    i = SQR(j);

    i = (j);
    
    return 0;
}
